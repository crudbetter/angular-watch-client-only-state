{
	"name": "angular-watch-client-only-state",
	"version": "0.0.0",
	"description": "Options for watching client only state with AngularJS",
	"repository": "https://github.com/crudbetter/angular-watch-client-only-state",
	"license": "MIT",
	"devDependencies": {
		"bower": "~1.3.9",
		"http-server": "~0.6.1"
	},
	"scripts": {
		"postinstall": "bower install",

		"prestart": "npm install",
		"start": "http-server -a localhost -p 8000 -c-1"
	}
}